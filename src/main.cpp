
#include <iostream>
#include "cg.h"

int main(int num_args, char** args)
{
    /*
    Constant* a = new Constant(1);
    ComputationNode* b = new Fraction;
    ComputationNode* c = new Fraction;
    ComputationNode* d = new Sum(1);
    ComputationNode* e = new Multiply;

    a->setValue(0, 10.0);
    b->connect(0, a, 0);
    c->connect(0, b, 0);
    e->connect(0, a, 0);
    e->connect(1, a, 0);
    d->connect(0, a, 0);
    d->connect(1, e, 0);
    d->update(0);

    std::cout << d->getOutput(0) << std::endl;
    */

    return 0;
}

